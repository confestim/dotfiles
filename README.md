## I have now switched to NixOS!

Running Niri!


## To install
Add the home-manager module to ~/.config/home-manager.

Add the nixos module to /etc/nixos/.
Then run `nixos-rebuild switch`.

Oneliner:
```bash
git clone https://github.com/confestim/dotfiles